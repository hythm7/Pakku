#!/usr/bin/env raku

use Test;

use Pakku::Repo;

my $repo;

my $name = 'site';

my $stage = $*HOME.add( '.pakku' ).add( 'stage' ).add( '.stage' );

lives-ok { $repo = Pakku::Repo.new: repo => $name, :$stage }, 'Create Pakku::Repo';

isa-ok $repo, Pakku::Repo, 'Pakku::Repo type';

done-testing;
